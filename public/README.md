# public/

Static assets served at the site root.

Drop the home hero background image here as **`hero-bg.jpg`** (referenced by
`components/home/home-hero.tsx` as `/hero-bg.jpg`, rendered at ~8% opacity).
Any web image works — rename it to `hero-bg.jpg`. The hero degrades gracefully
(brand-green tint) if the file is absent.
